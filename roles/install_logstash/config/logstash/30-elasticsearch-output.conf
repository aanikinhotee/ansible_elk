output {
  elasticsearch {
    hosts => ["dev-project1:9200", "dev-project2:9200", "dev-project3:9200"]
    sniffing => true
    manage_template => false
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
    document_type => "%{[@metadata][type]}"
  }
}
